server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # Client-side routing: an unknown path is a route for React Router to resolve,
  # not a 404 from nginx.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
