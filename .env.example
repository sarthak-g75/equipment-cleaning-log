# Copy to .env for docker compose. These values are wired into docker-compose.yml.
POSTGRES_USER=cleaning
POSTGRES_PASSWORD=cleaning
POSTGRES_DB=cleaning_log
POSTGRES_PORT=5433

API_PORT=4000
WEB_PORT=5173

JWT_SECRET=dev-only-secret-change-me
JWT_EXPIRES_IN=15m
